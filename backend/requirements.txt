# Pinned with compatible-release bounds (tested versions noted in comments).
fastapi~=0.136          # tested 0.136.3
python-multipart~=0.0.9 # multipart form uploads (cover upload)
uvicorn[standard]~=0.49 # tested 0.49.0
sqlmodel~=0.0.38        # tested 0.0.38
ebooklib~=0.20          # tested 0.20.0 — EPUB metadata + cover
pypdf~=6.0              # tested 6.13.1 — PDF embedded metadata
pymupdf~=1.27           # tested 1.27.2 — PDF first-page cover render
pillow~=12.0            # tested 12.2.0 — cover normalisation
pytest~=9.0             # tested 9.0.3
httpx~=0.28             # tested 0.28.1 — FastAPI TestClient
