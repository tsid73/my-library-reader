{
  "name": "my-library-reader",
  "private": true,
  "version": "0.1.0",
  "description": "One-command launcher for the backend + frontend.",
  "scripts": {
    "setup": "pip install -r backend/requirements.txt && npm --prefix frontend install",
    "dev": "concurrently -n api,web -c blue,magenta -k \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && python3 main.py",
    "dev:web": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm run build && cross-env MLR_SERVE_SPA=1 npm run dev:api",
    "test": "(cd backend && python3 -m pytest -q) && npm --prefix frontend test"
  },
  "overrides": {
    "shell-quote": "^1.8.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
